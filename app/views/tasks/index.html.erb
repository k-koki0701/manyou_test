<h1>タスク一覧</h1>
<table>
  <tr>
    <th>タスク名</th>
    <th>詳細</th>
  </tr>
<% @tasks.each do |task| %>
  <tr>
    <td><%= task.task_name %></td>
    <td><%= task.task_detail %></td>
  </tr>
<% end %>
</table>

<%= link_to '新しくタスクを登録する', new_task_path %>
