<h1>管理者画面</h1>

<% @users.each do |user| %>
  <table>
    <tr>
      <td><%= user.id %>：</td>
      <td><%= user.name %></td>
      <td><タスク数>：<%= user.tasks.count %></td>
    </tr>
  </table>
<% end %>
